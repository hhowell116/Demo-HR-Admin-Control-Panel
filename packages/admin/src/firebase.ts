// ─── DEMO MODE: Firebase mocked ────────────────────────
// No real Firebase connection — all data is static

export const auth = {} as any;
export const db = {} as any;

export function getAuthProvider(): any {
  return {};
}
